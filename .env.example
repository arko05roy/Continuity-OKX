DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require
PUBLIC_BASE_URL=
PAY_TO=
# Payment is intentionally not enabled in this first slice.
# Add OKX credentials only when wiring the real x402 middleware:
# OKX_API_KEY=
# OKX_SECRET_KEY=
# OKX_PASSPHRASE=
# PAY_TO=
